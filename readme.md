# Demo Webflux + MongoDb

# Levantar Mongo no docker

No docker-compose estão as configurações para levantar no docker as imagens de mongodb express, mongodb.

```sh
docker-compose -f docker-compose.yml up
```
